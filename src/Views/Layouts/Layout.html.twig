
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <header class="bg-white border-b border-gray-200 px-6 py-4 flex items-center justify-between">

    <!-- Left -->
    <div class="flex items-center space-x-10">
        <!-- Logo -->
        <div class="text-xl font-bold text-blue-600">
            JobPortal
        </div>

        <!-- Navigation -->
        <nav class="hidden md:flex items-center space-x-6 text-gray-600 font-medium">
            <a href="#" class="hover:text-blue-600">Dashboard</a>
            <a href="#" class="hover:text-blue-600">Messages</a>

            <!-- Recruteur -->
            <a href="#" class="hover:text-blue-600 recruteur-only">Offres</a>

            <!-- Admin -->
            <a href="#" class="hover:text-blue-600 admin-only">Utilisateurs</a>
        </nav>
    </div>

    <!-- Right -->
    <div class="flex items-center space-x-6">
        <!-- Role Badge -->
        <span class="px-3 py-1 text-xs font-semibold rounded-full
            bg-blue-100 text-blue-700">
            {% block title %} Admin {% endblock %} 
            <!-- Admin | Recruteur | Candidat -->
        </span>

        <!-- Notification -->
        <button class="relative text-gray-500 hover:text-blue-600">
            🔔
            <span class="absolute -top-1 -right-1 w-2 h-2 bg-red-500 rounded-full"></span>
        </button>

        <!-- Profile -->
        <div class="flex items-center space-x-3 cursor-pointer">
            <img
                src="https://i.pravatar.cc/40"
                alt="profile"
                class="w-9 h-9 rounded-full border"
            >
            <span class="hidden md:block text-gray-700 font-medium">
                Ali Kamal
            </span>
        </div>
    </div>

    
</header>

<main>
        {% block content %} {% endblock %}
    </main>


    <footer class="bg-white border-t border-gray-200 px-6 py-4 mt-auto">
    <div class="flex flex-col md:flex-row items-center justify-between gap-4">

        <!-- Left -->
        <p class="text-sm text-gray-500">
            © 2026 <span class="font-semibold text-gray-700">TalentHub</span>.
            Tous droits réservés.
        </p>

        <!-- Center -->
        <nav class="flex items-center space-x-6 text-sm text-gray-500">
            <a href="#" class="hover:text-blue-600">À propos</a>
            <a href="#" class="hover:text-blue-600">Support</a>
            <a href="#" class="hover:text-blue-600">Conditions</a>
            <a href="#" class="hover:text-blue-600">Confidentialité</a>
        </nav>

        <!-- Right -->
        <div class="flex items-center space-x-4 text-gray-500">
            <a href="#" class="hover:text-blue-600">🌐</a>
            <a href="#" class="hover:text-blue-600">🐦</a>
            <a href="#" class="hover:text-blue-600">💼</a>
        </div>

    </div>
</footer>

</body>
</html>